name: Import to crowdin

on:
  workflow_dispatch:

jobs:
  importAddons:
    uses: nvdaaddons/crowdinRegistration/./.github/workflows/importToCrowdin.yml@main
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        addon: ['clipContentsDesigner', 'pcKbBrl']
    with:
      ${{ matrix.addon }}
